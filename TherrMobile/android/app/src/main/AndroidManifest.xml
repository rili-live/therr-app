<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.READ_CONTACTS" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
  <!-- READ_MEDIA_IMAGES is intentionally omitted for the Google Play photo/video
       permissions policy. Image picking uses react-native-image-crop-picker's
       openPicker(), which goes through the Android system photo picker
       (ActivityResultContracts.PickVisualMedia) — per-item access with NO persistent
       media permission. If a transitive dependency ever re-injects this permission on
       manifest merge, escalate by declaring it here with tools:node="remove" (mirroring
       the location-permission removals documented below). -->
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <!-- Google Play Billing (react-native-iap / main/utilities/habitsBilling.ts).
       Declared explicitly rather than relying on manifest merge: react-native-iap
       ships an EMPTY android/src/main/AndroidManifest.xml, so the permission only
       arrives transitively from the Play Billing AAR pulled in by
       io.github.hyochan.openiap:openiap-google. Play Console will not let you create
       in-app products or subscriptions until it has an uploaded artifact declaring
       this permission, so an unlucky dependency change must not be able to silently
       drop it. Keep it here. -->
  <uses-permission android:name="com.android.vending.BILLING" />

  <!-- HABITS niche app does not use location. react-native-background-geolocation
       is removed from package.json on this branch (its Transistorsoft license is
       per-applicationId and does not cover com.therr.habits). react-native-geolocation-service
       is still bundled (shared with Therr/Teem) but gated by ENABLE_LOCATION_SERVICES=false.
       Strip the location permissions it would otherwise inject into the merged manifest
       so Play Store does not flag the app for undeclared location use. Do NOT port these
       removals to general/THERR/TEEM — those variants do use location. -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove" />
  <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" tools:node="remove" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" tools:node="remove" />

  <uses-feature
    android:name="android.hardware.camera"
    android:required="false" />
  <uses-feature
    android:name="android.hardware.camera.front"
    android:required="false" />

  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:usesCleartextTraffic="true"
    android:allowBackup="false"
    android:extractNativeLibs="false"
    android:theme="@style/AppTheme">
    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:exported="true"
      android:screenOrientation="portrait"
      android:theme="@style/BootTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter
        android:autoVerify="true"
        android:label="@string/filter_view_https_therr">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />

        <!--
          Hosts come from the appLinkHost/appLinkHostWww manifest placeholders in
          app/build.gradle, which picks them by applicationId. Hardcoding therr.com here
          meant the Friends with Habits build (com.therr.habits) never claimed
          habits.therr.com, so its own invite and pact links always opened in the browser.
        -->
        <data
          android:scheme="https"
          android:host="${appLinkHostWww}" />
        <data
          android:scheme="https"
          android:host="${appLinkHost}" />
      </intent-filter>
      <!--
        Push-notification click actions.

        FCM display notifications (push-notifications-service
        createNotificationMessage) carry an `android.notification.clickAction`.
        The Firebase SDK turns it into `new Intent(action).setPackage(appId)`;
        if no activity here declares that action, the notification still posts
        but tapping it does nothing at all.

        The action prefix is brand-specific — it comes from
        `PushNotifications.AndroidIntentActions` in therr-js-utilities
        (Therr: app.therrmobile, Teem: com.therr.mobile,
        Habits: com.therr.mobile.habits). Hardcoding `app.therrmobile` here
        meant the Friends with Habits build declared none of the actions its
        own backend sends, so every reminder, streak and pact notification was
        untappable. `notificationActionPrefix` is a manifest placeholder keyed
        off applicationId in app/build.gradle, same pattern as appLinkHost.

        Data-only notifications (createDataOnlyMessage) are rendered locally by
        Notifee with `launchActivity: 'default'` and do NOT need a filter here —
        but every type below is listed anyway, so flipping one from data-only to
        display on the backend can't silently reintroduce the dead-tap bug.
      -->
      <intent-filter>
        <action android:name="${notificationActionPrefix}.ACHIEVEMENT_COMPLETED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.CREATE_YOUR_PROFILE_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.CREATE_A_MOMENT_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.COMPLETE_DRAFT_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.LATEST_POST_LIKES_STATS" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.LATEST_POST_VIEWCOUNT_STATS" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_AREAS_ACTIVATED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NUDGE_SPACE_ENGAGEMENT" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.POST_VISIT_REVIEW_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_CONNECTION" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_CONNECTION_REQUEST" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_DIRECT_MESSAGE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_GROUP_MESSAGE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_GROUP_INVITE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_GROUP_MEMBERS" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_LIKE_RECEIVED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_SUPER_LIKE_RECEIVED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_THOUGHT_REPLY_RECEIVED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_THOUGHT_REPOST_RECEIVED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.UNREAD_NOTIFICATIONS_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.UNCLAIMED_ACHIEVEMENTS_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.INVITE_FRIENDS_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.REPORT_CONFIRMED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.LEADERBOARD_RANK_MILESTONE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>

      <!-- HABITS: pact lifecycle, partner activity, streaks and habit reminders. -->
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PACT_INVITATION" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PACT_NUDGE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PACT_ACCEPTED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PACT_DECLINED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PACT_COMPLETED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PACT_EXPIRING" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PARTNER_CHECKED_IN" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PARTNER_MISSED_DAY" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.PARTNER_CELEBRATED" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.STREAK_MILESTONE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.STREAK_AT_RISK" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.STREAK_BROKEN" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.NEW_PERSONAL_RECORD" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.DAILY_HABIT_REMINDER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.MORNING_MOTIVATION" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="${notificationActionPrefix}.EVENING_CHECK_IN" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>

      <!-- App shortcuts (long-press launcher icon). See res/xml/shortcuts.xml -->
      <intent-filter>
        <action android:name="app.therrmobile.QUICK_CREATE_MOMENT" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="app.therrmobile.QUICK_CREATE_THOUGHT" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>

      <meta-data
        android:name="android.app.shortcuts"
        android:resource="@xml/shortcuts" />
    </activity>

    <meta-data
      android:name="com.google.android.geo.API_KEY"
      android:value="${GOOGLE_APIS_ANDROID_KEY}" />

    <meta-data
      android:name="com.google.firebase.messaging.default_notification_icon"
      android:resource="@drawable/ic_notification_icon"
      tools:replace="android:resource" />
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_color"
      android:resource="@color/therr_blue"
      tools:replace="android:resource" />
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_channel_id"
      android:value="@string/default_notification_channel_id"
      tools:replace="android:value" />

    <!-- Suppress duplicate FileProvider authority. Both react-native-blob-util and
         react-native-image-crop-picker declare ${applicationId}.provider; on API 36
         this breaks activity registration. Image-crop-picker's Java calls
         FileProvider.getUriForFile(..., packageName + ".provider", ...) which then
         resolves to blob-util's FileProvider — whose file_paths cover the
         getExternalFilesDir(DIRECTORY_PICTURES) location where image-crop-picker
         writes camera temp files. -->
    <provider
      android:name="com.reactnative.ivpusic.imagepicker.IvpusicImagePickerFileProvider"
      tools:node="remove" />

    <uses-library
      android:name="org.apache.http.legacy"
      android:required="false" />
  </application>

  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="http" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="https" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="geo" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="google.navigation" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="applemaps" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="citymapper" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="uber" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="lyft" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="transit" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="truckmap" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="waze" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="yandexnavi" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="moovit" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="yandexmaps://maps.yandex." />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="yandextaxi" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="kakaomap" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="mapycz" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="mapsme" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="osmand.geo" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="gett" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="nmap" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="dgis" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="lftgpas" />
    </intent>
    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="petalmaps" />
    </intent>
  </queries>
</manifest>
