import * as current from './retro'; // Change this path to update use a different theme

const colors = current.colors;
const colorVariations = current.colorVariations;

export {
    colors,
    colorVariations,
};
