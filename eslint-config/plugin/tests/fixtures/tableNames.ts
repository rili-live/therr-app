// Fixture for no-unquoted-camelcase-table-in-raw-sql.test.js: what a service's
// src/store/tableNames.ts looks like, so the rule's one-hop import resolution is tested
// against a real file on disk.
export const PACTS_TABLE_NAME = 'habits.pacts';
export const USER_LOCATIONS_TABLE_NAME = 'main.userLocations';
export const TYPED_TABLE_NAME: string = 'main.users';
