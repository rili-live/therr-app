# Publish only the built dist/ (declared via package.json "files"), but be
# explicit about excluding source and dev artifacts in case "files" is relaxed.
src/
tests/
coverage/
.changeset/
.github/
*.config.ts
tsconfig.json
eslint.config.js
.prettierrc.json
