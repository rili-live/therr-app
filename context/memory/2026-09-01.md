# 2026-09-01

## Goal
Fix the right-hand header drawer rendering under the Android status bar / camera cutout (Pixel 8) after the RN upgrade.

## Deliverables
- `49b5f9a36` on `general` — `DrawerOverlay` in `TherrMobile/main/components/HeaderMenuRight.tsx` now pads by the measured `SafeAreaInsetsContext` inset (top + bottom), and its `Modal` sets `statusBarTranslucent` + `navigationBarTranslucent`.
- Drawer backdrop style moved to `styles/modal/headerMenuModal.ts` (`overlay`), dropping the static 48pt iOS pad on the shared `theme.styles.overlay`.
- New regression test `__tests__/components/HeaderMenuRightDrawer.test.tsx` (reanimated + notifee stubbed).

## Decisions
- Fixed the drawer only; the app header already reads insets (`Layout.tsx:2371`).
- Kept `theme.styles.overlay` untouched in case a niche branch consumes it.

## Open threads
- `npm run pr:tsc-baseline:mobile` fails on `Layout.tsx TS2339 Property 'pactEnded' does not exist on type 'typeof Types'` — pre-existing on `general`, unrelated to this change, still unfixed.
- User will merge `general` down into `niche/HABITS-general` themselves.
