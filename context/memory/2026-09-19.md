# 2026-09-19

## Goal

User got an admin email that a business claimed a space, but /dashboard-admin showed a
long list of unrelated "pending requests" and not the new claim. Root-cause or provide a
prod debug script.

## Findings (all confirmed, not inferred)

Four separate defects in the claim path, on `general` (branch
`claude/admin-dashboard-missing-claim-18rvdy`):

1. `claimSpace` (POST /spaces/request-claim/:spaceId — mobile "claim this space")
   called `updateSpace` without `fromUserId`. `updateSpace` puts `fromUserId` in its
   WHERE as an ownership check, so knex threw "Undefined binding(s)"; the handler's
   catch swallowed it and still returned 200. **Those claims were never written to the
   DB at all** — they exist only as the admin email. Verified against knex 3.3.
2. `SpacesStore.searchSpaces` ORed `isPublic = true` into the `isClaimPending` filter,
   then ordered by distance from Austin LIMIT 50 → the admin list was "the 50 public
   spaces nearest Austin". Explains both the misidentified rows and the missing claim.
3. `approveSpaceRequest` cleared the pending flag but never moved `fromUserId` to the
   claimant, and `updateSpace` silently drops `fromUserId` from its SET anyway. So
   `searchMySpaces` (keys on `fromUserId`) showed approved businesses nothing.
4. The non-super-admin guard in `searchSpaces` returned a plain object without touching
   `res` → hung the request.

Plus: sightengine re-flag scoped to `userId` not the row owner; ViewSpace read
`requestedByUserId` as "pending", pinning the banner to approved spaces.

## Decisions

- A claim on an **existing** space deliberately does NOT set `isClaimPending` — that
  would pull a live business off the map during review. The admin queue predicate is
  `isClaimPending = true OR (requestedByUserId IS NOT NULL AND fromUserId <> requestedByUserId)`,
  and approval transferring ownership is what clears shape 2 out of the queue.
- Ownership transfer lives in a new `SpacesStore.approveClaim`, deliberately NOT in
  `updateSpace` — letting that method write `fromUserId` would let a caller reassign the
  column that guards it.

## Deliverables

- Commit 3582c81d, pushed. 132 maps-service unit tests pass (5 new), dashboard + web
  jest pass, tsc clean for maps/dashboard/web, no new lint.
- `_bin/prod-debug/space-claims-audit.sql` — read-only audit, validated against a real
  PG 16 instance with stubbed PostGIS functions and fixtures for all six row shapes.

## Open threads

- Repair block in the audit script is **best run before this deploys**: those rows are
  only unambiguously identifiable until the fix ships, and unrepaired they show up in
  the corrected admin queue as fresh claims. Logged in WORK_IN_PROGRESS.md §
  Manual Operational Follow-ups.
- Claims lost to defect 1 have no backfill — reconcile the admin emails by hand.
- No PR opened (user did not ask).

## Session 2 — peer review of #2927 + request/claim split (afternoon)
- /quality-peer-review general→stage. Found and fixed: `claimSpace` accepted claims on
  business-owned spaces (now super-admin-only inventory), and — the big one — #2927's
  `approveClaim` COALESCE'd ownership for every shape, which would have handed consumer
  "Request a Space" suggestions to the consumer and left them permanently "pending".
- Decision (user): the admin queue is two lists. Space request (isClaimPending=true)
  → publish (consumer → unclaimed inventory, requestedByUserId cleared; business's own
  → keeps owner). Claim on existing space → transfer ownership. `approveClaim` decides
  by shape; dashboard shows them separately with "On approval: …" per row.
- a730f85b… is the user's personal account; its approved requests stay super-admin.
- Prod repair ran (release-all version) 14:42 UTC; verified read-only: 25 released,
  Pappadeaux recorded, 0 NULL geomCenter. WIP item ticked.
- Distinct consumer email ("Your suggested space is now live"), requester names in
  the dashboard queue, queue strings in the dashboard dictionary.
- Open: reconcile remaining claim-request emails (WIP item 2); Dave's Hot Chicken +
  Angelo Caputo's still pending under the personal account.

## Session 3 — prod incident digest + failed deploy migration step (evening)
- Digest (`_bin/prod-debug/.prod-debug/incident-20260919T155723Z.md`, NB not
  `.prod-debug/`): cluster healthy; only real error was users-service
  `relation "main.leaderboardperiodresults" does not exist` — lowercase = Postgres folding
  an unquoted camelCase table in `closePeriod`'s raw INSERT. Fails regardless of
  migration state. Fixed (c7761c6a) + SQL-inspecting test.
- Pasted knex "migration directory is corrupt, missing: <51 files>" = exactly
  maps-service's migration dir. Cause: push-notifications-service knexfile points at
  MAPS_SERVICE_DATABASE with an empty migrations dir. Dropped it from
  THERR_MIGRATABLE_SERVICES + pr:migrate:all (4cf1e51f). DB untouched (knex validates
  before running). Which CI step ran it is unconfirmed — under the deploy plan push
  should have been skipped as up-to-date.
- Harness (23dcefdc): `therr/no-unquoted-camelcase-table-in-raw-sql` on for every
  service; `quoteTableName()` added to therr-js-utilities/db; ContentMediaStore
  wrapped; documented in CLAUDE.md + DB guidelines. Only 2 sites repo-wide tripped it.
- Side observation, not chased: api-gateway deployment runs `:latest` while
  VERSIONS.txt says ee2e7ea7.
- Not pushed; no PR opened.

## Session 4 — /quality-peer-review of the incident fixes, then commit + push
- No bugs in the diff. Rule clean across every service + gateway (0 hits).
- Added `tests/integration/leaderboardPeriodResults.integration.test.ts`: runs
  `closePeriod` against real Postgres (the unit test stubs it — that is how the bug hid).
  Verified it reproduces `relation "main.leaderboardperiodresults" does not exist`
  against the pre-fix store. Local DB needed `pr:migrate:users` (was 9 behind).
- Rule: excluded `EPOCH FROM` / `IS DISTINCT FROM` from table position; import cache
  keyed on mtime for editor-hosted ESLint. Pushed to origin/general per user.
- Side observation still open: api-gateway deployment on `:latest` vs VERSIONS.txt.
