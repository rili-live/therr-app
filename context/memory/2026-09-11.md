# 2026-09-11

## Goal
Make shared check-ins show in the Habits feed: explain the feed model, put the
author's own share in their feed immediately, fix the prod "we couldn't share
this check-in" toast, and make the share toggle findable / default-on for
public profiles.

## Deliverables
- `general` d2e958c0b: `shareCheckin` activates the post for the author via
  `createReactions` with a pinning `relevanceScore` (1e6; stream orders
  `relevanceScore DESC NULLS LAST`) and returns the thought hydrated through
  `ThoughtsStore.find` (+ `reaction`, `likeCount`, `replies`).
- `general` 9fe2ea955: `HabitActions.shareCheckin` dispatches
  `INSERT_ACTIVE_THOUGHTS` on first share (`content` is redux-persist
  whitelisted, so it survives cold start).
- `niche/HABITS-general` 9b359f752: `CheckinProofSheet` — photo above note,
  share row always visible (disabled until photo), compact thumbnail preview,
  `defaultSharePublicly` from `user.settings.settingsIsProfilePublic`. 3 locales.
- UNCOMMITTED (stash@{0} on `general`): `k8s/prod/users-service-deployment.yaml`
  adds MAPS_SERVICE_GOOGLE_CREDENTIALS_BASE64, BUCKET_PUBLIC/PRIVATE_USER_DATA,
  SIGHTENGINE_API_KEY/SECRET (mirrors maps-service) + WIP backlog bullet. The
  auto-mode classifier refused to commit it (credential-leak heuristic).

## Decisions
- Feed is NOT friends-only: it is distributor-activated public HABITS-brand
  thoughts; it only looked friends-only because shared check-ins are the sole
  HABITS content source. `BRAND_THOUGHTS_VISIBILITY[HABITS] = [HABITS]`.
- Root cause of the share toast: users-service prod pod had no GCS bucket /
  credentials / Sightengine env → `storage.bucket('')` throws (500); side
  effect: every proof moderation fails closed → flagged.

## Open threads
- User must commit the stash on `general`, then general → stage → main.
- Post-deploy: verify share 201 + proofs `auto_verified`; re-moderate proofs
  flagged before the roll (WIP § Manual Operational Follow-ups 2026-09-11).
- Optional next: activate pact partners on share (closes thoughts.ts:294 TODO);
  a persistent "add photo" entry on today's check-in beyond the transient toast.

## Session 2 (peer review + release)
- `general` 0c2e5c1bb: committed the users-service k8s env fix (was stash@{0}); pushed.
  general→stage→main NOT yet done — the share endpoint stays 500 in prod until it rolls.
- /quality-peer-review (general→stage): no findings; users 1110 unit + 74 integration,
  therr-react 243, lint/tsc clean.
- `niche/HABITS-general` ← general merged, verify-brand clean; /quality-peer-review-niche:
  no leaks, no findings; mobile 1873 tests, tsc baseline 105=105, locales OK.
- `niche/HABITS-general` ee5705c4e: bump 1.6.1 / versionCode 40 + changelogs (3 locales);
  merged to `niche/HABITS-main` and pushed → EAS build triggered.

## Session: Habits mobile branding, first slice (branch claude/habits-mobile-brand-polish-okry0j off niche/HABITS-general)
- New `TherrMobile/main/components/Loaders/ChameleonLoader.tsx` (svg + reanimated, theme colours, logo geometry); `LottieLoader` and `EarthLoader` render it on HABITS for every id.
- `AchievementClaim` share message now translated with `{appName}` + `buildAchievementsShareUrl`; `buildShareUrl` drops locale prefixes on the habits host (fixed allowlist, no localised routes).
- Deferred: Habits achievement card Lotties still reuse Therr's (explorer/socialite/...) — needs a chameleon card set; `LottieLoader` "Loading..." caption is untranslated; AchievementClaim destructures route.params unguarded.
