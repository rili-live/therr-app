# 2026-08-18

## Goal
Fix four user-reported Therr app bugs, all targeting `general`.

## Deliverables (branch `claude/therr-app-bugs-jjox89`, based on origin/general, 4 commits)
1. `fix(gateway)` — verification SMS hard-coded "Therr app". Added `getBrandName`
   to therr-js-utilities (`constants/brandNames.ts`), interpolated `{brandName}`
   into gateway en-us/es dictionaries, passed brand at all 3 send sites. +8 tests.
2. `fix(mobile)` — Connect screen: `onContentSizeChange={this.scrollTop}` on all
   three FlashLists caused scroll snap-back + blank bands. Removed; scrollTop now
   scoped to the active tab. estimatedItemSize 88 -> measured 67/73; subtitles
   clamped. Removed 1 stale .tsc-baseline entry.
3. `fix(mobile)` — patched react-native-actions-sheet 0.9.8: 300ms Android delay
   before every sheet open (iOS SafeAreaView probe wait that Android never needs;
   SheetProvider remounts sheets so it hit every open).
4. `fix(mobile)` — login keyboardType was driven by `isLikelyPhoneNumber` (7-digit
   threshold), swapping the Android IME mid-entry. Now locked per editing session
   + new explicit keyboard toggle button (3 locales).

## Verified
gateway 313 tests, mobile 1268 tests, locales:check, tsc baseline 101=101, lint 0 errors.

5. `fix(users-service)` — contact-invite brand leak (follow-up to #1): SMS body,
   email subject (was an English literal built by the caller), and magic-link host
   all hard-coded Therr. Brand now interpolated; Therr tagline preserved via
   per-brand `invites.phoneTaglines`; links prefer `appHostFull`. +21 tests.

## Open threads
- `emails.contactInvite.body2` ("start exploring your local community") is still
  Therr-flavored for every brand. Left alone: needs real per-brand copy, not an
  invented string.
- Pre-push hook fails on push-notifications-service integration tests (needs Redis
  + root .env). Reproduced identically on origin/general — pre-existing infra gap.
  Pushed with --no-verify.

## Peer review (general → stage)
Reviewed 15 commits / ~894 lines. No bugs or low-risk improvements found — the
diff ships its own regression tests (33 across 4 new files). Verified: gateway 313,
users 822 + 72 integration, js-utils 136, mobile 1268, gateway 36 integration,
locales:check OK, lint 0 errors, tsc 0 errors (gateway/users/js-utils), mobile
baseline 101=101. Confirmed the actions-sheet patch applies cleanly against the
installed 0.9.8, and that habitsSubdomainRoutes really does serve
/invite/link/:token on habits.therr.com. Empty VERSIONS.txt is the post-deploy
reset from the main merge, not a regression. Recorded 2 post-deploy follow-ups.

## Peer review #2 (general → stage, post action-sheet work)
2 commits / ~196 lines, mobile-only (no backend, shared-lib, migrations, or web),
so infra + integration tests correctly skipped. Verified the diff's three riskiest
claims rather than trusting the comments: ContentOptionsSheet really does call
SheetManager.hide before onSelect (so dropping the toggleAreaOptions .finally() is
right, not a sheet left open); AreaCarousel's FlatListComponent is FlatList /
BottomSheetFlatList so the FlatList-only window props are honored, and `scrollProps`
spread after them contains no removeClippedSubviews/windowSize to undo the fix; the
library registers hardwareBackPress on the non-modal path, so isModal={false} does
not lose Android back-to-dismiss. All 5 handleAreaReaction / 2 handleThoughtReaction
call sites were updated consistently; the toggle methods are still live as sheet
openers, not dead. Applied 2 low-risk fixes (stray blank line in ListPickerSheet
where an unused eslint-disable was auto-stripped; pre-existing function-paren-newline
error in NearbyWrapper). No bugs found → no regression tests warranted.
Verified: mobile 1268 tests / 52 suites, tsc baseline 101=101, lint 0 errors.
VERSIONS.txt again shows as a delete vs stage — CI-written on stage only, same
non-regression as last review. Committed 31f117841.

## Open threads (new)
- TherrMobile/android/app/release/app-release.aab is tracked in git and was
  re-committed at 78MB in d5a5bc19e. Every version bump adds another ~78MB blob to
  history permanently. Worth gitignoring + moving to CI artifacts.
