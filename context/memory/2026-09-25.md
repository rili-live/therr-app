## Habit templates by category (branch claude/habits-template-categories-9b7voh)
- Goal: more FwH habit templates, browsable by category; templates name what, never when.
- Deliverable: 43 templates in 8 categories (fitness, money, mindfulness, health, productivity, learning, social, home). There is a new `habit_goals."templateKey"`, and the mobile picker has Popular + category tabs, translated from `pages.pacts.templates.<key>`.
- Decisions: templateKey plus client-side translation, not locale columns. "Morning workout" renamed in place to "Work out" 3x/wk. No therapist template. A clone stores the translated text.
- Open: cherry-pick a8b46013 + aab41f27 (backend + therr-react) onto general; the mobile commit stays niche.

## AR navigation plan (docs)
- Goal: roadmap a deprioritized AR / Android XR "View in AR" marker-navigation project.
- Deliverables: docs/AR_NAVIGATION_PLAN.md; index entry in docs/README.md; new "Future / deprioritized projects" table in PROJECT_BRIEF.md. Uncommitted on `general`.
- Decisions: ViroReact (@reactvision/react-viro) for phone ARCore, AR Optional; Jetpack XR native activity for headsets (reframed as spatial preview, since headsets are not a walking-navigation device); mobile Play track with xr.api.spatial required=false.
- Open: no GitHub issue filed; Phase 0 spike validates Viro on RN 0.86.

## Niche peer review + HABITS 1.11.0 (49) release
- Goal: /quality-peer-review-niche on niche/HABITS-general vs -main, then ship to Play.
- Deliverables: 6e41f479 (Celebration founder-offer impression logged when the offer lands late; start-cap strip copy names the rolling N-day window, required before showing), c6f285f9 (bump to 1.11.0/49 + changelogs), merged --no-ff into niche/HABITS-main (43a35071) and pushed both.
- Decisions: shipped ahead of the templateKey migrations. The client falls back to stored English names and the existing `category` column, and the WIP item on general already tracks the migration check.
- Open: promote general → stage → main so the template migrations run.

## Habit picker UX split (branch claude/habit-picker-configure-step, ab19cdc1)
- Goal: cadence/savings options sat below the fold on "Pick a habit", so users missed them; the radios were unbranded emoji.
- Deliverable: wizard steps are now pick → configure ("Make it yours") → partners → review. A template tap auto-advances, so there are no extra taps. Drawn brand radio cards and a Switch replace the emoji. Tests are updated, 86 pass, and it was verified on the emulator.
- Open: not pushed and no PR yet; it targets niche/HABITS-general (mobile-only).

## HABITS 1.12.0 (50) release
- Goal: build and release the FwH AAB.
- Deliverable: CI EAS build for merge c13592e4 failed because the EAS free-plan Android quota is exhausted until 10-01. Built locally with `gradlew bundleRelease` (signed with the FwH upload key, versionCode 50 verified) and uploaded with `eas submit --profile habits-internal` to an internal-track draft (submission 4cf0c27e). Preflight: GO.
- Open: roll out and promote in Play Console, paste the notes, and decide on the EAS plan. Both are in the WIP follow-ups (uncommitted).

## /niche-android-release skill
- Goal: a single command to check the version, merge to niche/HABITS-main, watch CI/EAS, and fall back to a local build + submit.
- Deliverables: `.claude/skills/niche-android-release/SKILL.md`, `_bin/niche-android-release.js` (check/merge/watch/build/submit), `_bin/lib/niche-android-release.js` + a test with 24 cases. check, watch and build were verified live against vc50. Uncommitted. It belongs on `general` (`_bin/**`).
- Decisions: ledger tags `habits-android-vc<N>` record local uploads. An existing EAS build always beats the CI state. Local builds rebuild the shared libs first.
- Open: backfill `habits-android-vc50`? Commit to general and merge down.
