# 2026-08-23

## Goal
Peer review `general` → `stage` (deploy pipeline rewrite: service registry,
per-service VERSIONS.txt ledger, desired-state deploy plan), then apply every
suggestion/recommendation from the review and a sub-agent's follow-up review.

## Deliverables (pushed to origin/general)
- 3eb38f697 — two silent-skip bugs + regression tests
  - run-migrations.sh parsed the deploy plan row with a whitespace IFS; an empty
    running tag dropped a column, emptied $VERDICT, and fell back to the HEAD^1
    merge diff. Now '|'-separated. (Tab does NOT work — read() strips leading
    fields for any all-whitespace IFS.)
  - plan_verdict probed image existence before the up-to-date check, and
    missing-image blocks. During the ledger transition every service resolves to
    LAST_PUBLISHED_GIT_SHA, only ever pushed for the services that merge rebuilt —
    so an already-correct service could abort the whole deploy.
- 827fd7443 — hardening + CI
  - publish.sh empty-index commit guard; service_dir() + two registry assertions;
    deploy.sh unpullable-image warning (gated on the cheap manifest probe) and
    per-line `kubectl apply` output match.
  - _bin/.eslintrc.js (root:true) — _bin had no config anywhere above it.
  - New CircleCI job `deploy_pipeline_gates` (registry/waves/test:bin-scripts on
    every branch). None of those gates were in CI at all.
- 36192cbb0 — stray ellipsis in users-service README.

## Decisions
- `**/lib/**` exclusions also swallow `_bin/lib/**`. Fixed in three places:
  lint-changed-files.sh, .husky/pre-commit, quality-peer-review SKILL.md. This hid
  1,230 of 1,831 changed lines from the first pass of this very review.
- Did NOT rewrite publish.sh's push path. A CircleCI job rerun still fails
  non-fast-forward (fresh checkout at CIRCLE_SHA1 vs the ledger commit already on
  origin/stage). Pre-existing, not introduced by the guard — logged as a WIP item.

## Open threads
- docs/WORK_IN_PROGRESS.md has 3 new unchecked follow-ups: watch the first
  stage→main deploy's plan table, confirm PUBLISHED_* rows appear afterward, and
  the publish-rerun reconciliation above.
