# 2026-07-21

#### Session 1
**Goal**: Engagement feature — ranked feed + auto-expanded thought threads (Areas route), improved thought activation algorithm.
**Deliverables**:
- users-service: `ThoughtsStore.find` withReplies → LATERAL top-3 reply previews (message/author) + true `replyCount` via `COUNT(*) OVER ()`; `getRecentThoughts` → hot-score ranking (reply count ÷ age gravity) over a bounded 200-row recent pool, parents only; `TherrEventEmitter.runThoughtDistributorAlgorithm` bug fix (was making up to 11 duplicate createReactions calls for the same user — now one deduped call, interests-first).
- reactions-service: `searchActiveThoughts` attaches `likeCount` to reply previews, filters blocked-user replies.
- TherrMobile: new `main/utilities/feedRanking.ts` (recency decay × log-engagement × category affinity from user's own likes; author-diversity pass; `getTopReply`/`shouldAutoExpandThread`); `getActiveCarouselData` supports `'ranked'`; Areas Discoveries+Thoughts tabs use it; `ThoughtDisplay` renders inline `ThreadPreview` (top reply + "View all N replies"); styles in thoughts/viewing.ts; locale strings in all 3 dictionaries (`components.thoughtDisplay.viewAllReplies`).
- Tests: ThoughtsStore (15 pass), feedRanking jest (13 pass); full suites green (users 353, reactions 105, mobile routes/components 550).
- docs/FEATURES.md updated.
**Decisions**: Ranking is client-side over the fetched page (cheap, no server round-trip); server-side "activation" ranking upgraded in-DB only (reply count is the only engagement signal in users DB — likes live in reactions DB; cross-service aggregation deferred). Auto-expand criteria: 2+ replies always, 1 reply needs a like signal. Reply previews capped at 3 via LATERAL to bound payload.
**Open threads**: Not committed (awaiting user go-ahead; backend and mobile must be separate commits per CLAUDE.md). Possible follow-ups: periodic like-count sync into thoughts table for richer server ranking; extend ranked feed to web Explore hub; impression-based downranking of already-seen posts.

#### Session 2
**Goal**: Bot personality redesign for therr-ai-automator (branch claude/bot-personality-redesign-4rxh52; no prior work existed on the branch)
**Deliverables**: 1 commit on therr-ai-automator — new `src/config/personas.ts` (structured BotPersona: identity, voice, interests, tonePool, emoji/hashtag propensities; 12 legacy botType keys preserved), rewired config/prompts/generators, new persona test suite (117 tests passing). PR: therr-ai-automator #2. therr-app: `005_bot_users.js` seed (10 persona-matched bot accounts, fixed b-prefix UUIDs, ON CONFLICT DO NOTHING, `BOT_SEED_PASSWORD` override) + WORK_IN_PROGRESS follow-up bullet; PR #2618.
**Decisions**: Persona resolved per user — known botType wins, unknown/'default' hashes user id to a stable persona (replaces the everyone-is-'a Foodie' fallback). Tone drawn from persona tonePool (not global list) for per-account consistency. Category selection biased 70% toward persona interests. republican/democrat keys kept for DB compat but reframed personality-first (regional/civic identity, not partisan bait). therr-app untouched — botType is an unconstrained string column there.
**Open threads**: `LLM_TONES` moved to personas.ts (re-exported from config). Fallback thoughts still pick uniform random category. Persona interests limited to ThoughtCategories subset. Seed run on prod is a manual post-deploy step (tracked in WORK_IN_PROGRESS follow-ups). republican/democrat personas have no seeded account (10 requested; 12 personas exist).
