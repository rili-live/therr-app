# 2026-08-04

## Goal
Make the mobile "Edit Profile" button land on the User Profile section of Settings instead of the top of the screen.

## Deliverables
- `UserDisplayHeader.tsx`: edit-profile button now navigates with `{ scrollToSection: 'userProfile' }`.
- `Settings/index.tsx`: measures the User Profile section via `onLayout`, scrolls to it on mount or on param change, then clears the param via `navigation.setParams`.
- 4 new tests in `__tests__/routes/Settings.test.tsx` (38 pass); lint clean; mobile tsc baseline still 104/104.

- Fixed the "add your name" prompt: it pointed at ManageAccount (whose only action is delete/deactivate account) and now focuses the first-name input rendered a few rows below it.
- `components/Input/index.tsx`: added an `inputRef` prop on BaseInput, forwarded to the Paper TextInput — a plain `ref` resolves to the BaseInput class instance, so there was previously no way to focus one of these inputs programmatically.

## Decisions
- Measured offset over a hardcoded one — sections above User Profile vary in height by locale, theme, and conditional links (business account, missing-name prompt).
- Handled both orderings (param-before-layout on mount, layout-before-param on re-navigate) since Settings is reused when already in the stack.

## Open threads
- Working tree still carries the earlier uncommitted Settings section reorder (content settings moved above notifications); nothing committed this session.
