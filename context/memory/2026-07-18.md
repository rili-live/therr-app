# 2026-07-18

#### Session 1
**Goal**: Implement Duolingo-style leaderboard for Therr Mobile, usable by Friends with Habits
**Deliverables**: 4 commits on `claude/therr-mobile-leaderboard-tsok73` — backend (main.userLeaderboardScores + settingsIsLeaderboardEnabled migrations, UserLeaderboardScoresStore in shadow mode, XP hooks in createOrUpdateAchievement + habit check-ins, GET /users/leaderboards, gateway route), therr-react UsersService.getLeaderboard, mobile Leaderboard screen (week/all-time × everyone/friends tabs) + Achievements entry banner + 3-locale strings, docs.
**Decisions**: Rank by new append-only weekly XP (not spendable coins — exchange decrements them). XP from applied achievement progress (5/unit + achievement.xp completion bonus, idempotent) for Therr; direct check-in (10 XP) + streak-milestone hooks for Habits since HABITS has no allow-listed achievement classes (a55bce9, deliberate). Global+Friends scopes, Monday-UTC weekly reset + all-time, default-in with opt-out. User rejected clarifying questions ("continue") → used recommended defaults.
**Open threads**: Post-deploy: run the two 2026-07-18 migrations; flip store shadow→enforce after a clean cycle. No Settings UI toggle yet for settingsIsLeaderboardEnabled (backend supports it). Leagues (cohorts/promotion) deferred; schema supports layering. Mobile tsc noise + App.test.tsx failure are pre-existing environment issues.

#### Session 2
**Goal**: Split backend/frontend branches; add rank-milestone push notifications, new achievement classes, FWH rendering, error handling
**Deliverables**: `claude/therr-leaderboard-backend-tsok73` (backend + shared libs: weeklyChampion class, HABITS allow-list re-enabled, leaderboardRankMilestone push end-to-end, 401/fire-and-forget hardening); `claude/therr-mobile-leaderboard-tsok73` restacked on it (mobile only: deep link press action, Lottie mapping, loading/offline/error states, locale parity ×3).
**Decisions**: Mobile branch STACKS on backend branch (mobile needs the shared-lib enums/classes to build). Rank milestones [10,3,1], edge-triggered crossing detection (no re-notify inside top N), opt-out respected. HABITS allow-list reversal of a55bce9 per user request — QA note added to WORK_IN_PROGRESS. Lazy require breaks achievements↔leaderboards module cycle.
**Open threads**: Merge order matters: backend branch first, then mobile. weeklyChampion Lottie reuses influencer-card until a trophy asset ships.
