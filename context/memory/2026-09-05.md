# 2026-09-05

## Session — Android notification small icon shows Therr logo (niche/HABITS-general)

### Goal
Friends with Habits pushes render the Therr "T" in the Android status bar (reported on
the habits daily-reminder push, "one check-in gets your streak going"). Should be the
chameleon glyph. Branch `claude/android-notification-icon-qhh28p`, based on
`origin/niche/HABITS-general`.

### Root cause
`ic_notification_icon` is byte-identical between `general` and `niche/HABITS-general` —
it had simply never been re-branded. Nothing in a niche build overrides drawables (no
product flavors; brand selection is `brandConfig.ts` + per-branch assets), so both push
paths land on the Therr mark: `AndroidManifest.xml`'s
`com.google.firebase.messaging.default_notification_icon` for notification-payload
messages, and notifee's `smallIcon` default in `main/utilities/pushNotifications.ts` for
data-only ones. push-notifications-service also sets `icon: 'ic_notification_icon'`
explicitly in `firebaseAdmin.ts`, same resource name resolved per-app — so nothing
backend needed to change. It is *not* every notification type; it is all of them.
Invisible because only the collapsed 24dp glyph is wrong — the expanded shade shows the
correct app icon and name.

### Deliverables
- `drawable-anydpi-v24/ic_notification_icon.xml` + the five density PNGs, regenerated as
  the chameleon silhouette.
- `TherrMobile/resources/icons/build-notification-icon.py` — derives the silhouette from
  `main/assets/habits-logo.svg` (shapely booleans, cairosvg raster).
- `TherrMobile/__tests__/assets/notificationIcon.test.ts` — 9 assertions; verified it
  fails (3 cases) against the Therr mark it replaced.

### Decisions
- The launcher icon can't be reused. Android reads the small icon's **alpha only** and
  repaints every opaque pixel, and the logo's eyes/stripe/smile are color contrast on an
  opaque body, so flattened it is a blob. Silhouette rule: body+ink opaque, white sclera
  punched out, pupils added back — `(head ∪ rims) − scleras ∪ pupils`.
- Dropped the forehead stripe. Nostrils and smile are dark-on-dark and vanish for free;
  the stripe is light-on-dark so keeping it means cutting it, and at 24dp the cut lands
  on the head's peak and reads as damage. Compared both at 24/36/48/72/96px.
- 22.5dp of artwork in the 24dp canvas — the footprint the Therr mark already used, so
  the two brands' status-bar icons read at one size.
- Generated, not hand-drawn: the path data is otherwise an unrecoverable blob. Same
  posture as `resources/fonts/fix-icon-font-bearings.py`.
- Kept the PNGs in sync even though they never ship (`minSdkVersion` 25, `anydpi`
  outranks every density qualifier) — a stale Therr mark sitting in `drawable-xxhdpi/`
  is a landmine.
- `values/colors.xml` `therr_blue` is already the habits purple `#3B2A4E` on this branch,
  so the notification accent color needed nothing. Name is a legacy misnomer; left alone.

### Open threads
- Not verified on a device. Geometry was checked by rasterizing the committed vector and
  diffing it against the committed PNGs (identical at 24 and 96px), and by eye on light
  and dark grounds at all five densities.
- Jest/ESLint could not be run in this container — no `node_modules` anywhere, and a full
  mobile install pulls the licensed `react-native-background-geolocation`. The new suite
  was run green under a standalone jest + ts-jest against the real files; it imports only
  `fs`/`path`, so nothing RN-specific is untested.
- iOS is unaffected (uses the app icon), and `general` deliberately keeps the Therr mark.

---

## Session — /work-plan habits, after merging general into niche/HABITS-general

### Goal
Bring `general` down into `niche/HABITS-general` and run `/work-plan habits`. Branch
`claude/habits-work-plan-setup-8zzv5u` (niche-based) + `…-8zzv5u-shared` (general-based).

### The merge
Clean, no conflicts — 29 files, all shared/backend plus three mobile files from the
thought-media work. `verify-brand` clean: all 7 identity files untouched, no icon/splash
or gradle diff, `CURRENT_BRAND_VARIATION` still HABITS. This is the merge the root
CLAUDE.md warns silently un-brands the app; it did not this time.

### Batch chosen — Tier 2 § 2.6, "finish the habits push last mile"
Three items, split across two branches per the repo's own rule.

**[general] The push toggles shipped inert.** `c45a0bc5` (yesterday, niche branch) added
`settingsPushHabitReminders` / `settingsPushStreakAlerts` switches to
ManageNotifications. Both write-path gates in users-service are explicit allow-lists —
`updateArgs` in `handlers/users.ts` and the param filter in `UsersStore.updateUser` —
and neither named the columns. The PUT returned 202, the store dropped the values, and
the screen showed its success toast. A user could turn the evening nudge off, be told it
saved, and keep getting it. Both allow-lists now carry the pair.

**[niche] `PACT_ENDED` was in no channel bucket.** So the data-only push carrying the
"Start New Cycle" CTA — the only prompt the whole fixed-cycle design produces, at the
only moment renewal is legal — rendered on "General" at DEFAULT importance, no heads-up
banner. Now in `REMINDER_ACTION_KEYS` beside `PACT_EXPIRING`.

**[general] The gate that should have caught it was crying wolf.**
`check-push-wiring.js` derived its bucketed-key set from two hardcoded names and missed
`CONTENT_DISCOVERY_ACTION_KEYS`, so 5 of its 6 findings were false. Now derived from
every `*_ACTION_KEYS` declaration.

### Decisions
- `!= null`, not truthiness, on the two new store params. The digest mutes on an explicit
  `false` and nothing else, so `false` is the only value that changes anything — a
  truthiness guard would have looked like a fix and left the feature broken.
- `PACT_ENDED` → `reminders` (HIGH) rather than `contentDiscovery`. It is one step past
  `PACT_EXPIRING` in the same lifecycle, not partner-activity chatter. User chose this
  over the Friend Activity option.
- Left `LEADERBOARD_RANK_MILESTONE` alone. It is unbucketed on `general` but bucketed on
  the niche branch — a Therr-side product call about whether a rank move should interrupt,
  and outside the habits batch that found it. Recorded as a follow-up.
- Did not touch § 2.6.5 (leaderboard XP sourcing — a product decision) or § 2.6.8
  (blocked on private-bucket thought media).

### Corrections to the backlog
§ 2.6.3 and the 2026-09-01 follow-up both claimed the `pactEnded` mobile half was not
built. It is — intent filter in `7db85fb`, `renew-pact` handler in `Layout.tsx`. Both
corrected; someone would otherwise have rebuilt it.

### Verification
users-service: 1058 passing, tsc clean, 0 lint errors. Mobile: 112 suites / 1851 tests
passing, tsc baseline 105 = 105, lint clean, locales OK. Wiring check: 0/0 on the niche
branch, unchanged on general. Root + TherrMobile `node_modules` and the two shared libs
had to be built first — nothing is preinstalled in this container.

### Open threads
- Neither half is verified on a handset. The two counters (`remindersMutedByPreference`,
  `lastChanceMutedByPreference`) leaving 0 is the only evidence the toggle pair is wired,
  and both branches must be out for that.
- Android locks channel importance at first creation, so the PACT_ENDED change only
  reaches installs that had not already created `default` for it.
- Structural: the HABITS channel buckets live only on `niche/HABITS-general`, so
  `general`'s `getAndroidChannelFromClickActionId` is permanently a subset. Worth deciding
  deliberately rather than by accretion.
