# 2026-07-30

## Goal
Make therr-app self-aware of the four sibling repos (`therr-ai-automator`,
`therr-messaging-automator`, `therr-infra-terraform`, `therr-landing`) so future code
changes here account for out-of-repo consumers. Docs only, on `general`.

## Deliverables
- New `docs/CROSS_REPO_INTEGRATION.md` — repo map, the table-by-table coupling surface for
  both Cloud Functions (regenerated 2026-07-30), expand/contract migration rule, brand-scoping
  mirror rule, future-dated `main.thoughts`, the 3-repo habits digest network path, Cloud
  Scheduler 3-job limit, and a "does my change need a sibling repo?" checklist.
- Root `CLAUDE.md`: new "## Sibling Repos" section (4 rules that actually bite) + index entry.
- Cross-references added in `docs/ARCHITECTURE.md` (Overview + Data Layer), `docs/README.md`,
  `docs/NICHE_APP_DATABASE_GUIDELINES.md` (new "Out-of-repo consumers" under Migration
  Coordination), `docs/MULTI_BRAND_ARCHITECTURE.md` (Key Integration Points), and
  `docs/PROD_DEBUG_CLAUDE.md` ("It only sees the GKE cluster" + 2 failure signatures).
- `.claude/skills/db-migration-scaffold/SKILL.md`: new Step 4.5 cross-repo consumer check
  (grep both automators before scaffolding a rename/drop on `main.*`) + a Rules bullet.

## Decisions
- Sourced from `~/Code/therr-workspace/docs/CROSS_REPO_ARCHITECTURE.md` but re-verified every
  claim against the repos (table greps, `k8s/prod` LB IP 10.128.15.235, netpol 10.128.0.0/20,
  `HOT_SCORE_EXPRESSION` GREATEST clamp, ai-automator `numHours` default 30).
- Wrote a therr-app-centric doc rather than pointing at the workspace repo — the workspace is
  local-only and its symlinks are gitignored, so it can't be a dependency for this repo's docs.
- Did not repeat the workspace's stale "therr-app CI has no type-check step" claim; CI does
  type-check now.
- Uncommitted — user hasn't asked to commit.

## Open threads
- Highest-value gate still missing: a schema contract test in each automator running its store
  queries against a migrated schema in CI. Nothing verifies the coupling today.
- `main.userLeaderboardScores` is brand-scoped here and read by neither automator (verified) —
  re-check on the next `BRAND_SCOPED_TABLES` addition.
