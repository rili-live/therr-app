# 2026-07-30

## Goal
Resolve conflicts from merging `general` into `niche/HABITS-general`, then commit + push.

## Deliverables
- Merge commit `db4557151` — 5 conflicts resolved:
  - `.circleci/config.yml` — kept HABITS `habits-internal` submit comment, took general's `no_output_timeout: 40m`.
  - `TherrMobile/android/app/build.gradle` — kept niche version stream (11 / 0.4.0), not general's 438 / 3.11.0.
  - `Layout.tsx` — combined niche featureFlags gating with general's `content?.activeX` path fix (`content?.content?.activeX` was wrong; `content` is `IContentState`).
  - `CreateProfile/index.tsx` — kept both imports.
  - `docs/WORK_IN_PROGRESS.md` — kept both follow-up blocks.
- `TherrMobile/.tsc-baseline` regenerated (105 → 108). General's baseline switched to the
  identity format and can't contain niche-only errors (PactCard/MyHabits/HabitDetail/PactDetail).
  Two Layout TS2367 brand-comparison errors dropped out (brand is HABITS here).
- `81c1882d2` — `Settings.test.tsx` theme assertion made brand-aware. It had been failing on
  this branch since `89006cf76` (HABITS maps `retro` → `dark`); general's test asserts `retro`.

## Decisions
- Baseline `--update` on niche is legitimate: the niche superset is expected, not new errors.
- Fixed the pre-existing test failure rather than `git push --no-verify`.

## Open threads
- Any future `general` → niche merge will re-conflict on the Settings theme assertion hunk.

## Session 2 — /quality-peer-review-niche (pre-Play-build review)
Scope: `niche/HABITS-general` vs `origin/niche/HABITS-main` — 48 commits, 131 files.

- Leak check clean: only `_bin/switch-brand.sh` differs from `origin/general`, and it is
  pre-existing (already on `-main`), not introduced in review scope.
- Backwards-compat clear: `origin/general` == `origin/main` (0 commits ahead), so every
  backend/shared-lib dep of this mobile build is already deployed. Nudge endpoint
  (`PUT /pacts/:id/nudge`) is live.
- Bug found + fixed (`094396bd3`): `PactsList.handleNudge` assumed HTTP success == nudge
  sent. Server returns 200 with cooldown/error inside `nudgeResults`, so users saw
  "Nudge sent!" when nothing went out; the catch branch used toast type `errorToast`,
  absent from App.tsx `toastConfig`, so failures rendered nothing at all.
  Added `nudgeError` to all 3 locales + `PactsListNudgeOutcome.test.tsx` (5 tests,
  fails 3/5 pre-fix).
- BLOCKER surfaced: Android versionCode still 11 on both HEAD and `-main`. Merging to
  `-main` as-is means a duplicate-versionCode Play rejection.

## Open threads (session 2)
- Nudge button never re-offers after server's 7-day cooldown (`canNudge: !nudgedAt`).
  Backend already returns `nextNudgeAvailableAt` to drive a re-enable.
- Follow-ups staged at scratchpad `wip-followups.md`; must land on `general`'s
  docs/WORK_IN_PROGRESS.md (niche edits to that file are dead code).
