# 2026-08-31

## Goal
Answer whether a post-deploy UAT could verify push notifications end-to-end in
production, then build it (steps 1-3 of the proposed design).

## Deliverables
Branch `claude/push-notification-uat-testing-n1semv`, based on `origin/general`
(rebased off main — general had newer edits to firebaseAdmin.ts, config.yml and
WORK_IN_PROGRESS.md that a main-based PR would have reverted). Commit 2d78283.

- `dryRun` option on `predictAndSendNotification` + `viaProductionPath` flag on
  both `send-test` endpoints, so a check can validate the envelope production
  actually sends rather than the raw path's narrower one.
- Dry runs are side-effect free (no `clearInvalidDeviceToken`) and log under
  distinct wording so they don't pollute the runbook's delivery-rate log query.
- `_bin/cicd/uat-push.sh` (3 checks) + `post_deploy_uat` CircleCI job on `main`.
- `--production-path` on push-debug.sh; runbook + CLAUDE.md + backlog updated.

## Decisions
- Raw path stays the default on send-test (better for debugging one handset);
  every response now reports `sendPath` so a green result says which ran.
- Job ships disabled — no credentials means "explain and pass", so an
  unconfigured gate never blocks a deploy. Config steps in § Manual Ops.
- Corrected mid-task: claimed the SENDABLE gate was an active divergence; the
  two sets are actually identical today. Replaced that test with a parity
  invariant that keeps them so.

## Open threads
- User must set UAT_SUPER_ADMIN_EMAIL/PASSWORD (+ optional UAT_USER_ID) in
  CircleCI, and decide on a second invocation for HABITS.
- Link 5 (device ack via uatPingId) not built — would split to a niche PR.
- No PR opened (not requested). PR must target `general`.
