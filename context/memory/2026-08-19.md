
## Peer review #3 (general → stage, pact-nudge honesty + cross-brand labels)
9 commits / ~894 lines across users-service, therr-react types, TherrMobile, and
the habits landing page. Infra healthy; users unit (834) + integration (72),
therr-react (229), mobile (1277 / 53 suites), web (322), locales all green;
tsc 0 errors everywhere, mobile baseline 101=101.

Found one Category A bug the diff's own thesis missed: dispatchPactInvitation
returned invitedVia:'sms' even when getSmsSender resolved to undefined (no Twilio
number configured for the country), so classifyDispatchResult counted the partner
as nudged and nudgePact marked them — burning the 7-day cooldown on a message
nobody received. Fixed by resolving the sender before persisting anything and
returning undeliverableReason:'noContactMethod'; other callers only branch on
isOnBrand so they are unaffected. Added a regression test. Committed 5bce1916c.

Verified rather than trusted: ErrorCodes.NOT_FOUND/NOT_PERMITTED/BAD_REQUEST all
exist; ThoughtsStore.find/getById have no explicit .select() so brandVariation
does reach the client (the cross-brand label is not a silent no-op); no client
consumes IPactNudgeResult.reason yet, so adding 'undeliverable' is additive.

## Open threads (new)
- sendPactInvitationEmail is still fire-and-forget behind a logging .catch — an
  SES failure reports the invite as delivered. Same bug class, async, unfixed.
- settingsEmailInvites is selected in dispatchPactInvitation's findUser columns
  and never consulted.
- Notifications and DirectMessage are still FlashList with a single
  estimatedItemSize over variable-height rows — the exact pattern this diff just
  reverted in Connect for causing blank bands. DirectMessage (60px over message
  bubbles) is the likelier of the two.
