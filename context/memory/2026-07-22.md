# 2026-07-22

## Session 1
**Goal**: VP-of-product retention audit of HABITS app; fix invite/signup gaps; regression-test invited-user→inviter connection on first login.
**Deliverables**: `docs/niche-sub-apps/habits/RETENTION_AUDIT.md`; `users-service/src/handlers/helpers/inviteAcceptance.ts` (+14 regression tests); fixes in `auth.ts`, `users.ts`, `pacts.ts`, `InvitesStore.ts`; mobile `Layout.tsx` refresh of activePacts after pact claim. Branch `claude/niche-retention-audit-w0t8zh`.
**Decisions**: Invite acceptance now yields a COMPLETE userConnection (invite = explicit consent, skip PENDING ceremony); all 3 invite paths (email/SMS invite, username referral, PACT-XXXX claim) connect inviter↔invitee; signup-time pact claim now mirrors acceptPact (streaks + creator push).
**Open threads**: Backend commits 72cc64a, 3ddb08f, 3e75ca7, 0049cb0 + web b109a4f must be cherry-picked to `general` to deploy. Pre-existing test failures: TherrMobile App.test/Settings; push-service Redis integration — all environmental.

## Session 2 (same conversation)
**Goal**: Implement all remaining audit items.
**Deliverables**: Streak freezes + gap-reset + same-day double-count fix (habitCheckins/streakHelpers/StreaksStore, streaks NEVER reset before!); claim auto-verify (isClaimCodePreVerified, skips verification email); 9 funnel.* MetricNames + recordFunnelMetric across register/verify/login/invite/pact/checkin; daily digest endpoint POST /habits/pacts/digest/run-daily (internal-only, streakAtRisk/partnerMissedDay/pactExpiring); firebaseAdmin apns-topic per brand (20 call sites) + brand icon color; ClaimPactLanding Play Store link fixed (was Therr app id, now com.therr.habits). 16 new regression tests; 379 passing.
**Open threads**: Ops — schedule daily digest cron (~23:00 UTC) + HABITS FCM creds (WORK_IN_PROGRESS follow-ups); on-device push QA; Play Install Referrer attribution; surface freezes in StreakWidget UI.
