# 2026-09-22

## Goal

Groom `docs/WORK_IN_PROGRESS.md`: merge duplicates, delete finished work, improve
wording, and align the `general` and `niche/HABITS-general` copies.

## Deliverables

- `8f3ddc855` on `claude/groom-work-in-progress-8nqehc` (based on `general`).
  3,249 → ~3,020 lines; 438 deleted / 215 added.

## Verified-done work removed (checked against code, not against the file)

- **Savings mobile half** — all six items shipped in `b126ba0b2` (#2941) on
  `niche/HABITS-general`: `goalType` clone in `CreatePactInvite`, amount field on
  `CheckinDetailForm`, `SavingsProgressCard`/`PactDetail`/`HabitDetail` totals,
  wizard target fields, `habit-checkin-savings` press action in `index.js`.
  Mobile locales at full parity: 2,214 keys in each of en-us/es/fr-ca.
- **2026-08 deploy-ledger outage** (4 entries, 1 incident) — `VERSIONS.txt` has
  all 8 `PUBLISHED_*` rows at `58a22c50e`.
- **"Reconcile WIP.md between branches"** — already true; niche is a strict
  subset of general.
- All 14 `[x]` entries in § Manual Operational Follow-ups.

## Duplicates merged (10 pairs)

Habits digest scheduler (×2), Play RTDN refund/lapse (×2 → one Pub/Sub consumer),
reaction-metric audits (×2), iOS version bump (×2), APNS-topic verification (×2),
habits sitemap (×3), Android release notes (×4, all pinned to stale versionCodes).

## Findings worth keeping

- **Mechanical bug in the file's own append protocol.** § How to maintain this
  document quoted `<!-- skill-followups:end -->` literally, so a skill appending a
  follow-up matched the prose instead of the real block and wrote an entry there.
  Fixed; marker names now spelled without their comment delimiters so exactly one
  literal copy of each exists. Worth checking if it recurs.
- The "more than one digest run per day duplicates pushes" warning carried by two
  entries is **false** — `notificationQueue`'s UNIQUE (brandVariation, userId,
  dedupeKey) makes retries safe.
- `HABITS_PLAY_LISTING.md` and `AUTOMATOR_HABITS_PLAN.md` exist only on
  `niche/HABITS-general`; referenced from `general`'s backlog, where they 404.

## Open threads

- Branch alignment is **not finished**. Land this on `general`, then merge
  `general` → `niche/HABITS-general`. That merge produces **exactly one**
  conflict, in the savings block (niche amended it in `6ffd5879a`, this commit
  deletes it) — resolve toward `general`, take the deletion. Simulated with
  `git merge-file`; no other hunk conflicts.
- Versions at time of writing: `general` `versionCode 459 / 3.17.6`; niche
  `45 / 1.8.0`; iOS `MARKETING_VERSION 1.71.0 / 213`.
- 6 open GitHub issues (#2923, #2922, #2909, #2908, #2841, #2840). #2840/#2841
  are correctly cross-linked to § 2.6.7 / § 2.6.8; the other four are
  issue-only, which is per convention.
