# 2026-09-22

## Goal
`/work-plan --niche habits` on `claude/niche-habits-work-plan-ls7xya` (sits exactly at
`origin/general`, so general-based: brandConfig is THERR, no `routes/Habits`).

## Plan outcome
Surfaced the live outage first: the three 2026-09-20 habits migrations **still have not
run on `main`**, so Friends with Habits check-ins have been 500ing for two days. No code
fixes that — it is a `kubectl exec`.

Took the Tier 3 § 3.5 batch that closes the *hazard* instead, over the habits-product
runner-up (#2908 + #2909 + #2923, daily-streak hardening — deferred deliberately: writing
more habits code while check-ins are down is the wrong order).

## Deliverables (commit 8d1b3e71, pushed, no PR — not asked for)
- `deploy.sh` traps `deploy_waves` instead of letting `set -e` end the job, migrates only
  services confirmed on their desired tag, still exits non-zero. Wave-ordering intent
  preserved.
- `run-migrations.sh`: per-service isolation; `verify_no_pending_migrations` (asks each pod
  directly, so it catches work left by *any* earlier deploy, not just this one); standalone
  `--service` / `--all` / `--verify-only`.
- `migrations:status` (knex `migrate:list`) on the 4 migratable services.
- `_bin/lib/tests/run-migrations-scope.test.js` — 14 assertions.
- Pre-push unblocked: mocharc timeout + the Redis teardown/cleanup hangs.

## Decisions
- User chose "migrate what rolled, then fail" over aborting pre-cluster, and yes to
  standalone mode.
- `MIGRATE_ONLY_SERVICES` set-but-empty ≠ unset. Collapsed with `${VAR:-}`, a first-wave
  failure would migrate *everything* underneath code that never moved. Tested explicitly;
  confirmed non-vacuous by reproducing the naive variant.
- Verify parse fails closed. Unknown `migrate:list` format ⇒ fail the job, never "0 pending".

## Gotchas found
- The backlog's pre-push diagnosis was **incomplete**. `quit()` rejecting at status `wait`
  is only one shape; against a host that is not listening ioredis sits at `connecting` and
  `quit()` *hangs*. The actual hook timeout came from `cleanupTestData` (`client.keys()`
  queued forever) — called from an `after` hook that lacks the `skipTests` guard its
  `beforeEach` has, in both suites. Guarded at module level. A/B proved 2 failing → 0.
- Container had no `node_modules`; root install + building `therr-js-utilities` was needed
  before any tsc. `--legacy-peer-deps` churns `package-lock.json` — reverted, not committed.

## Open threads
- **The three migrations are still unrun.** Guard cannot help until it reaches `main`.
- #2840 corrected by comment: the code half shipped 2026-09-05; only the web render
  (`ViewThought.tsx`, `ThoughtCard.tsx` — no `medias` reference) and the orphaned-uploads
  decision remain. Left open. Knock-on: #2841's stated blocker is largely gone; its real
  one is unmoderated `habits.proofs`.
