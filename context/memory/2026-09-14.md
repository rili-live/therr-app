# 2026-09-14

## Goal
Verify Google Ads API access after the Cloud Console access change; prove the `therrads` tool can read and create.

## Findings
- Access works: reads (`campaign list`, `report ads`) and full-validation Mutates succeed on 7604290203 via manager 3076709152.
- Google moved API access levels from the Ads UI API Center to Cloud Console (APIs & Services -> Google Ads API).
  Developer token no longer required by Google, but google-ads lib 29.x still requires the config key.
- google-ads 29.2 defaults to API v23, which broke three never-exercised shapes in the apply path:
  campaign.start_date -> start_date_time ("yyyy-MM-dd HH:mm:ss"); contains_eu_political_advertising is now
  REQUIRED on create; standalone campaign.target_cpa is rejected for Search (use maximize_conversions.target_cpa_micros).
  Plus two latent Python bugs (_TempIds eager dict; proto-plus has no CopyFrom).
- Added `campaign apply --validate-only` (API validate_only=True) — both specs pass; nothing created.
- GA4 half needs ADC with analytics.readonly; the 2026-09-09 Play re-login dropped it. Workaround:
  GOOGLE_APPLICATION_CREDENTIALS=~/.therr/ga-mcp-sa.json. Documented in README.

## Open threads
- Changes are uncommitted in scripts/google-ads on niche/HABITS-general; must land on `general`.
- `campaign budget` / `resume` dry-runs were not exercised (auto-mode classifier blocked them).

## Follow-up: GA4 auth
- `gcloud auth application-default login --scopes=...analytics.readonly` fails with "This app is blocked":
  Google blocks gcloud's shared OAuth client from that scope. Unrelated to the Ads API Basic-access upgrade.
- Fix shipped: settings.yaml -> ga4.credentials_file (service-account JSON; ~/.therr/ga-mcp-sa.json works).
  ADC is only the fallback. Play README reverted to the three Play scopes.
