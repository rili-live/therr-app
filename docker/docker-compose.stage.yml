version: '3.6'

services:
  certbot:
    image: certbot/certbot
    restart: unless-stopped
    volumes:
      - ./volumes/certbot/conf:/etc/letsencrypt
      - ./volumes/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - rili-network
  rili-nginx:
    image: rili/nginx-stage
    environment:
      - NGINX_HOST=stage.rili.live
    volumes:
      - ./volumes/certbot/conf:/etc/letsencrypt
      - ./volumes/certbot/www:/var/www/certbot
      - ./volumes/nginx.stage.conf:/etc/nginx/nginx.conf
  rili-client-web:
    image: rili/client-web-stage
    build:
      args:
        - NODE_ENV=stage
    environment:
      - NODE_ENV=stage
    volumes:
      - ./volumes/certbot/conf:/etc/letsencrypt
      - ./volumes/certbot/www:/var/www/certbot
  rili-server-api:
    image: rili/server-api-stage
    build:
      args:
        - NODE_ENV=stage
    environment:
      - NODE_ENV=stage
    volumes:
      - ./volumes/certbot/conf:/etc/letsencrypt
      - ./volumes/certbot/www:/var/www/certbot
  rili-server-socket:
    image: rili/server-socket-stage
    build:
      args:
        - NODE_ENV=stage
    environment:
      - NODE_ENV=stage