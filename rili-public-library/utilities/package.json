{
  "name": "rili-public-library-utilities",
  "version": "1.0.0",
  "description": "Helper utilities to provide common functions that were abstracted for re-use.",
  "main": "index.js",
  "scripts": {
    "build": "../../node_modules/.bin/webpack --env production",
    "build:dev": "../../node_modules/.bin/webpack --env development",
    "lint:fix": "../../node_modules/.bin/eslint --ext .jsx,.js --fix ./",
    "lint": "../../node_modules/.bin/eslint --ext .jsx,.js ./",
    "test": "npm run lint:fix && echo \"Error: no test specified\" && exit 0"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
