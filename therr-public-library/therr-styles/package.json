{
  "name": "therr-public-library-styles",
  "version": "0.0.1",
  "description": "Multi-purpose code used in the Therr codebase and open-sourced to the developer community",
  "main": "index.js",
  "scripts": {
    "build": "../../node_modules/.bin/sass lib:lib",
    "build:dev": "../../node_modules/.bin/sass lib:lib",
    "build:watch": "../../node_modules/.bin/sass --watch lib:lib",
    "lint:fix": "../../node_modules/.bin/stylelint '**/*.s+(a|c)ss' --fix --custom-syntax 'postcss-scss' --max-warnings 0",
    "lint": "../../node_modules/.bin/stylelint '**/*.s+(a|c)ss' --custom-syntax 'postcss-scss' --max-warnings 0",
    "test": "echo \"Error: no test specified\" && exit 0",
    "test:coverage": "echo \"Error: no test:coverage specified\" && exit 0"
  },
  "keywords": [
    "therr",
    "public",
    "library"
  ],
  "author": "Therr, Inc.",
  "license": "MIT",
  "devDependencies": {}
}
